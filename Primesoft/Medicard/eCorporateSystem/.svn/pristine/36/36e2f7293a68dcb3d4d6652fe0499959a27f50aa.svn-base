@model IEnumerable<Batch>
<h4 class="form-title">Batch List</h4>
@*<table>
    <thead>
        <tr>
            <th class="header-cell col">Account Code</th>
            <th class="header-cell col">Date Uploaded</th>
            <th class="header-cell col">Uploaded By</th>
            <th class="header-cell col">Status</th>
            <th class="header-cell col">&nbsp;</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td class="body-cell col">
                    @Html.HiddenFor(modelItem => item.Guid)
                    @Html.DisplayFor(modelItem => item.AccountCode)
                </td>
                <td class="body-cell col">
                    @item.DateUploaded.ToString("MM/dd/yyyy hh:mm tt")
                </td>
                <td class="body-cell col">
                    @Html.DisplayFor(modelItem => item.UploadBy)
                </td>
                <td class="body-cell col">
                    @Html.DisplayFor(modelItem => item.Status)
                </td>
                <td class="body-cell col">
                    <a href="javascript:void(0);" onclick="findBatch($(this));" id="findBatch" class="btn btn-primary pull-right btn-xs">Apply</a>
                </td>
            </tr>
        }
    </tbody>
</table>*@

        <table class="table table-fixed-left">
            <thead>
                <tr>
                    <th style="width: 20%">Account Code</th>
                    <th style="width: 20%">Date Uploaded</th>
                    <th style="width: 30%">Uploaded By</th>
                    <th style="width: 20%">Status</th>
                    <th style="width: 10.3333%">&nbsp;</th>
                    <th style="width: 0.6666">&nbsp;</th>
                </tr>
            </thead>
        </table>

        <div class="scrollable-table-fixed" style="margin-top: -3px !important;margin-bottom: 0 !important">
            <table class="table table-fixed-left">
                <tbody>
                    @foreach (var item in Model)
        {
            <tr>
                <td style="width: 20%">
                    @Html.HiddenFor(modelItem => item.Guid)
                    @Html.DisplayFor(modelItem => item.AccountCode)
                </td>
                <td style="width: 20%">
                    @item.DateUploaded.ToString("MM/dd/yyyy hh:mm tt")
                </td>
                <td style="width: 30%">
                    @Html.DisplayFor(modelItem => item.UploadBy)
                </td>
                <td style="width: 20%">
                    @Html.DisplayFor(modelItem => item.Status)
                </td>
                <td style="width: 10%">
                    <a href="javascript:void(0);" onclick="findBatch($(this));" id="findBatch" class="btn btn-primary pull-right btn-xs">View Data</a>
                </td>
            </tr>
        }

                </tbody>
            </table>
        </div>